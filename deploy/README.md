# 🤖 تثبيت تطبيق ذكاء على سيرفر VPS

## المتطلبات
- سيرفر Ubuntu 20.04 أو أحدث
- 8GB RAM على الأقل
- صلاحيات root

## خطوات التثبيت

### الخطوة 1: الاتصال بالسيرفر
```bash
ssh root@عنوان_IP_السيرفر
```

### الخطوة 2: تحميل ملفات التطبيق
```bash
# إنشاء مجلد مؤقت
mkdir -p /tmp/thakaa-setup
cd /tmp/thakaa-setup

# تحميل الملفات (انسخ جميع الملفات من مجلد التطبيق)
```

### الخطوة 3: تشغيل سكريبت التثبيت
```bash
chmod +x install.sh
sudo ./install.sh
```

### الخطوة 4: إدخال البيانات المطلوبة
- كلمة سر قاعدة البيانات (اختر كلمة سر قوية)
- مفتاح Groq API الخاص بك

## بعد التثبيت

التطبيق سيعمل تلقائياً على:
```
http://عنوان_IP_السيرفر
```

## أوامر مفيدة

```bash
# عرض حالة التطبيق
pm2 status

# عرض السجلات
pm2 logs thakaa

# إعادة تشغيل التطبيق
pm2 restart thakaa

# إيقاف التطبيق
pm2 stop thakaa
```

## إضافة نطاق (Domain)

إذا كان لديك نطاق خاص:

1. وجه النطاق إلى IP السيرفر في إعدادات DNS

2. عدل ملف Nginx:
```bash
sudo nano /etc/nginx/sites-available/thakaa
```

3. غير `server_name _;` إلى `server_name yourdomain.com;`

4. أعد تشغيل Nginx:
```bash
sudo systemctl restart nginx
```

5. أضف شهادة SSL مجانية:
```bash
sudo certbot --nginx -d yourdomain.com
```

## الدعم

إذا واجهت مشاكل، تحقق من:
- السجلات: `pm2 logs thakaa`
- حالة PostgreSQL: `sudo systemctl status postgresql`
- حالة Nginx: `sudo systemctl status nginx`
